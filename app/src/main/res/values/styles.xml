<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="basetext">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="numbertext" parent="basetext">
        <item name="android:textSize">17sp</item>
    </style>
    <style name="edit_text_custom">
        <item name="android:textColorHint">@color/colorPrimaryBlur</item>
        <item name="android:textColor">@color/color_main</item>
        <item name="android:background">@drawable/bg_edit_custom</item>
        <item name="android:textSize">15sp</item>
    </style>
    <style name="text_view_profile">
        <item name="android:textColor">@color/color_gg</item>
        <item name="android:background">@drawable/bg_edit_custom</item>
        <item name="android:textSize">15sp</item>
    </style>
</resources>
